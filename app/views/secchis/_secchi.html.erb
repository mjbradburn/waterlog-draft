    <tr data-toggle="collapse" data-target=".sample<%=secchi.id%>">
      <td><div class="btn-group-vertical">
        <button type='button' class='btn btn-info' data-toggle='collapse' data-target=".sample<%=secchi.id%>">Details</button>


        <%= link_to 'Edit', edit_secchi_path(secchi), class: 'btn btn-mini btn btn-warning', remote: true %>


        <%= link_to 'Destroy', secchi, class: 'btn btn-mini btn btn-danger', method: :delete, data: { confirm: 'Are you sure?' }, remote: true%>
      </div></td>

      <td><%= Lake.find(secchi.lake_id).lkname %> </td>
      <td><%= secchi.sample_time.to_s(:short) %></td>
    </tr>

    <tr>
      <%=render 'show', :secchi => secchi %>
    </tr>