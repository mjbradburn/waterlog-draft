<%= form_for(@lake) do |f| %>
  <% if @lake.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lake.errors.count, "error") %> prohibited this lake from being saved:</h2>

      <ul>
      <% @lake.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lkname %><br>
    <%= f.text_field :lkname %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
